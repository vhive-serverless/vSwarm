apiVersion: serving.knative.dev/v1
kind: Service
metadata:
  name: auth-python
  namespace: default
spec:
  template:
    spec:
      containers:
        - image: docker.io/vhiveease/auth-python:latest
          imagePullPolicy: Always
          ports:
            - name: h2c
              containerPort: 50051
